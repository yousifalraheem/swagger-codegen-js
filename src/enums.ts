export namespace Enums {
    export enum ValueTypes {
        URL = "URL",
        PATH = "PATH",
        TEMPLATE = "TEMPLATE",
        SCHEMA = "SCHEMA",
        OUTPUT_PATHS = "OUTPUT_PATHS",
        UNKNOWN = "UNKNOWN"
    }
    
    export enum SourceTypes {
        EXTERNAL = "EXTERNAL",
        LOCAL = "LOCAL",
        RAW = "RAW",
        UNKNOWN = "UNKNOWN"
    }
}